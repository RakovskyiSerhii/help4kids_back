// // bin/server.dart
// import 'dart:io';
// import 'package:dart_frog/dart_frog.dart';
// import 'package:help4kids/routes/_handler.dart';
//
// Future<void> main() async {
//   // Read the port from environment variables (default to 8080)
//   final port = int.tryParse(Platform.environment['PORT'] ?? '8080') ?? 8080;
//   final ip = InternetAddress.anyIPv4;
//
//   // Start the server using the global `handler` (automatically generated by Dart Frog)
//   final server = await serve(handler, ip, port);
//   print('Server listening on http://${server.address.address}:${server.port}');
// }